export type Category = {
  id: number;
  name: string;
  description: string;
};

export type CreateCategoryData = {
  name: string;
  description: string;
};
export type UpdateCategoryData = {
  name: string;
  description: string;
};
